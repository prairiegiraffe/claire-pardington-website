---
import Layout from '~/layouts/Layout.astro';
import DevComponentsSidebar from '~/components/ui/DevComponentsSidebar.astro';
import CompleteLandingPage from '~/components/ui/page-sections/landing-pages/CompleteLandingPage';
import NavigationLandingPage from '~/components/ui/page-sections/landing-pages/NavigationLandingPage';
import BackgroundImageLandingPage from '~/components/ui/page-sections/landing-pages/BackgroundImageLandingPage';
import TestimonialsLandingPage from '~/components/ui/page-sections/landing-pages/TestimonialsLandingPage';
import { getComponentsBySubcategory } from '~/lib/component-registry';

const metadata = {
  title: 'Landing Pages — Component Library',
  description: 'Collection of complete landing page components for development reference.',
  robots: { index: false, follow: false },
};

// Get components for this subcategory
const components = getComponentsBySubcategory('landing-pages');
---

<Layout metadata={metadata}>
<DevComponentsSidebar>
  <div class="container mx-auto px-4 py-8 max-w-7xl">
    <!-- Header -->
    <div class="mb-8">
      <div class="flex items-center gap-2 text-sm text-muted mb-2">
        <a href="/dev-components" class="hover:text-primary">Component Library</a>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
        </svg>
        <span>Landing Pages</span>
      </div>
      <h1 class="text-4xl font-bold mb-4">Landing Pages</h1>
      <p class="text-lg text-muted max-w-3xl">
        Complete landing page templates with clear AI naming conventions. Perfect for creating full marketing pages with
        multiple sections.
      </p>
    </div>

    <!-- Component Library -->
    <div class="space-y-16">
      <!-- Complete Landing Page Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700 relative">
          <CompleteLandingPage client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Complete Landing Page</h3>
              <p class="text-muted mb-4">
                Full-featured landing page with hero, features, stats, CTA, and footer sections
              </p>

              <div class="text-sm space-y-2">
                <div>
                  <strong>AI Reference:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >complete-landing-page</code
                  >
                </div>
                <div>
                  <strong>Component:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >CompleteLandingPage</code
                  >
                </div>
                <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {
                    components
                      .find((c) => c.id === 'complete-landing-page')
                      ?.tags.map((tag) => (
                        <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                      ))
                  }
                </div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Dependencies:</h4>
                <div class="flex flex-wrap gap-1">
                  {
                    components
                      .find((c) => c.id === 'complete-landing-page')
                      ?.dependencies?.map((dep) => (
                        <span class="text-xs bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 px-2 py-1 rounded font-mono">
                          {dep}
                        </span>
                      ))
                  }
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre
                class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<CompleteLandingPage
  title="Deploy to the cloud with confidence"
  subtitle="Anim aute id magna aliqua ad ad non deserunt sunt..."
  ctaText="Get started"
  ctaHref="#"
  secondaryCtaText="Learn more"
  secondaryCtaHref="#"
  logoSrc="your-logo-url"
  screenshotSrc="your-screenshot-url"
  announcementBadge="What's new"
  announcementText="Just shipped v1.0"
  companyName="Your Company, Inc."
/>`}</code>
              </pre>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Key Features:</h4>
                <ul class="text-sm text-gray-600 dark:text-gray-400 space-y-1">
                  <li>• Hero section with gradient background and dual CTAs</li>
                  <li>• Logo cloud for social proof</li>
                  <li>• Primary and secondary feature sections</li>
                  <li>• Statistics section with key metrics</li>
                  <li>• Final CTA section with gradient background</li>
                  <li>• Complete footer with social links</li>
                  <li>• Full dark mode support</li>
                  <li>• Responsive design for all screen sizes</li>
                  <li>• Customizable content and branding</li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Navigation Landing Page Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700 relative">
          <NavigationLandingPage client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Navigation Landing Page</h3>
              <p class="text-muted mb-4">
                Complete landing page with navigation, hero, features, testimonials, pricing, FAQs, and footer
              </p>

              <div class="text-sm space-y-2">
                <div>
                  <strong>AI Reference:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >navigation-landing-page</code
                  >
                </div>
                <div>
                  <strong>Component:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >NavigationLandingPage</code
                  >
                </div>
                <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {
                    components
                      .find((c) => c.id === 'navigation-landing-page')
                      ?.tags.map((tag) => (
                        <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                      ))
                  }
                </div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Dependencies:</h4>
                <div class="flex flex-wrap gap-1">
                  {
                    components
                      .find((c) => c.id === 'navigation-landing-page')
                      ?.dependencies?.map((dep) => (
                        <span class="text-xs bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 px-2 py-1 rounded font-mono">
                          {dep}
                        </span>
                      ))
                  }
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre
                class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<NavigationLandingPage
  title="Deploy to the cloud with confidence"
  subtitle="Anim aute id magna aliqua ad ad non deserunt sunt..."
  ctaText="Get started"
  ctaHref="#"
  secondaryCtaText="Learn more"
  secondaryCtaHref="#"
  logoSrc="your-logo-url"
  screenshotSrc="your-screenshot-url"
  companyName="Your Company"
  navigationItems={[
    { name: 'Product', href: '#' },
    { name: 'Features', href: '#' }
  ]}
/>`}</code>
              </pre>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Key Features:</h4>
                <ul class="text-sm text-gray-600 dark:text-gray-400 space-y-1">
                  <li>• Full navigation header with mobile menu</li>
                  <li>• Hero section with gradient backgrounds</li>
                  <li>• Logo cloud for social proof</li>
                  <li>• Feature section with icon grid</li>
                  <li>• Testimonial section with background image</li>
                  <li>• Complete pricing section with 3 tiers</li>
                  <li>• FAQ section with grid layout</li>
                  <li>• Final CTA section with gradient effects</li>
                  <li>• Comprehensive footer with navigation</li>
                  <li>• Interactive mobile navigation with Headless UI</li>
                  <li>• Full dark mode support</li>
                  <li>• Responsive design for all screen sizes</li>
                  <li>• Customizable content and branding</li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Background Image Landing Page Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700 relative">
          <BackgroundImageLandingPage client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Background Image Landing Page</h3>
              <p class="text-muted mb-4">
                Landing page with background image hero, collapsible FAQs, and side-by-side pricing
              </p>

              <div class="text-sm space-y-2">
                <div>
                  <strong>AI Reference:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >background-image-landing-page</code
                  >
                </div>
                <div>
                  <strong>Component:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >BackgroundImageLandingPage</code
                  >
                </div>
                <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {
                    components
                      .find((c) => c.id === 'background-image-landing-page')
                      ?.tags.map((tag) => (
                        <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                      ))
                  }
                </div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Dependencies:</h4>
                <div class="flex flex-wrap gap-1">
                  {
                    components
                      .find((c) => c.id === 'background-image-landing-page')
                      ?.dependencies?.map((dep) => (
                        <span class="text-xs bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 px-2 py-1 rounded font-mono">
                          {dep}
                        </span>
                      ))
                  }
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre
                class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<BackgroundImageLandingPage
  title="Data to enrich your online business"
  subtitle="Anim aute id magna aliqua ad ad non deserunt sunt..."
  ctaText="Get started"
  ctaHref="#"
  secondaryCtaText="Learn more"
  secondaryCtaHref="#"
  logoSrc="your-logo-url"
  companyName="Your Company"
  heroBackgroundImage="your-background-image-url"
  announcementText="Announcing our next round of funding."
  testimonialQuote="Your testimonial quote..."
  testimonialAuthor="Judith Black"
  testimonialTitle="CEO of Tuple"
/>`}</code>
              </pre>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Key Features:</h4>
                <ul class="text-sm text-gray-600 dark:text-gray-400 space-y-1">
                  <li>• Hero section with customizable background image</li>
                  <li>• Full navigation header with mobile menu</li>
                  <li>• Logo cloud for social proof</li>
                  <li>• Feature section with screenshot preview</li>
                  <li>• Split testimonial section with image and quote</li>
                  <li>• Two-tier pricing with side-by-side layout</li>
                  <li>• Collapsible FAQ section using Headless UI Disclosure</li>
                  <li>• Comprehensive footer with navigation</li>
                  <li>• Interactive mobile navigation</li>
                  <li>• Gradient overlays and visual effects</li>
                  <li>• Full dark mode support</li>
                  <li>• Responsive design for all screen sizes</li>
                  <li>• Customizable content and branding</li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Testimonials Landing Page Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700 relative">
          <TestimonialsLandingPage client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Testimonials Landing Page</h3>
              <p class="text-muted mb-4">
                Complete landing page with navigation, hero, testimonials grid, features, newsletter, and footer
              </p>

              <div class="text-sm space-y-2">
                <div>
                  <strong>AI Reference:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >testimonials-landing-page</code
                  >
                </div>
                <div>
                  <strong>Component:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >TestimonialsLandingPage</code
                  >
                </div>
                <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {
                    components
                      .find((c) => c.id === 'testimonials-landing-page')
                      ?.tags.map((tag) => (
                        <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                      ))
                  }
                </div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Dependencies:</h4>
                <div class="flex flex-wrap gap-1">
                  {
                    components
                      .find((c) => c.id === 'testimonials-landing-page')
                      ?.dependencies?.map((dep) => (
                        <span class="text-xs bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 px-2 py-1 rounded font-mono">
                          {dep}
                        </span>
                      ))
                  }
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre
                class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<TestimonialsLandingPage
  navigationItems={[
    { name: 'Product', href: '#' },
    { name: 'Features', href: '#' },
    { name: 'Marketplace', href: '#' },
    { name: 'Company', href: '#' }
  ]}
  companyName="Your Company"
  logoUrl="your-logo-url"
  logoUrlDark="your-dark-logo-url"
  heroTitle="A better way to ship your projects"
  heroDescription="Your description here..."
  ctaText="Get started"
  ctaSecondaryText="Learn more"
  featuredTestimonial={{
    body: "Your featured testimonial...",
    author: {
      name: "Brenna Goyette",
      handle: "brennagoyette",
      imageUrl: "profile-image-url",
      logoUrl: "company-logo-url"
    }
  }}
/>`}</code>
              </pre>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Key Features:</h4>
                <ul class="text-sm text-gray-600 dark:text-gray-400 space-y-1">
                  <li>• Hero section with app screenshot mockup</li>
                  <li>• Full navigation header with mobile menu</li>
                  <li>• Logo cloud for social proof</li>
                  <li>• Dark feature section with product screenshot</li>
                  <li>• Secondary features grid with icons</li>
                  <li>• Newsletter signup section</li>
                  <li>• Extensive testimonials grid with featured testimonial</li>
                  <li>• Multi-column testimonial layout</li>
                  <li>• Comprehensive footer with social media links</li>
                  <li>• Interactive mobile navigation</li>
                  <li>• Gradient overlays and visual effects</li>
                  <li>• Full dark mode support</li>
                  <li>• Responsive design for all screen sizes</li>
                  <li>• Customizable content and branding</li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </section>
    </div>

    <!-- Navigation -->
    <div class="mt-12 flex justify-between items-center">
      <a href="/dev-components" class="inline-flex items-center text-primary hover:text-primary/80 font-medium">
        ← Back to Component Library
      </a>
      <a href="/styles-guide" class="inline-flex items-center text-muted hover:text-primary font-medium">
        Styles Guide →
      </a>
    </div>
  </div>
</DevComponentsSidebar>
</Layout>
