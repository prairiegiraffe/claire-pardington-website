---
import Layout from '~/layouts/Layout.astro';
import DevComponentsSidebar from '~/components/ui/DevComponentsSidebar.astro';
import WithStackedFlyoutMenu from '~/components/ui/elements/headers/WithStackedFlyoutMenu';
import Constrained from '~/components/ui/elements/headers/Constrained';
import OnBrandBackground from '~/components/ui/elements/headers/OnBrandBackground';
import WithFullWidthFlyoutMenu from '~/components/ui/elements/headers/WithFullWidthFlyoutMenu';
import FullWidth from '~/components/ui/elements/headers/FullWidth';
import WithCallToAction from '~/components/ui/elements/headers/WithCallToAction';
import WithMultipleFlyoutMenus from '~/components/ui/elements/headers/WithMultipleFlyoutMenus';
import WithIconsInMobileMenu from '~/components/ui/elements/headers/WithIconsInMobileMenu';
import WithLeftAlignedNav from '~/components/ui/elements/headers/WithLeftAlignedNav';
import WithRightAlignedNav from '~/components/ui/elements/headers/WithRightAlignedNav';
import WithCenteredLogo from '~/components/ui/elements/headers/WithCenteredLogo';
import { getComponentsBySubcategory } from '~/lib/component-registry';

const metadata = {
  title: 'Headers — Component Library',
  description: 'Collection of navigation header components for development reference.',
  robots: { index: false, follow: false },
};

// Get components for this subcategory
const components = getComponentsBySubcategory('headers');
---

<Layout metadata={metadata}>
  <DevComponentsSidebar>
    <div class="container mx-auto px-4 py-8 max-w-7xl">
      <!-- Header -->
      <div class="mb-8">
        <div class="flex items-center gap-2 text-sm text-muted mb-2">
          <a href="/dev-components" class="hover:text-primary">Component Library</a>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
          <span>Headers</span>
        </div>
        <h1 class="text-4xl font-bold mb-4">Headers</h1>
        <p class="text-lg text-muted max-w-3xl">
          Navigation header components with clear AI naming conventions. Perfect for creating professional site headers
          with mobile support.
        </p>
      </div>

      <!-- Component Library -->
      <div class="space-y-16">
        <!-- With Stacked Flyout Menu Component -->
        <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
          <!-- Component Preview -->
          <div class="border-b border-gray-200 dark:border-gray-700">
            <WithStackedFlyoutMenu client:load />
          </div>

          <!-- Component Info -->
          <div class="p-6 bg-gray-50 dark:bg-gray-800">
            <div class="grid lg:grid-cols-3 gap-6">
              <div class="lg:col-span-1">
                <h3 class="text-xl font-semibold text-primary mb-2">With Stacked Flyout Menu</h3>
                <p class="text-muted mb-4">Navigation header with stacked flyout menu and mobile support</p>

                <div class="text-sm space-y-2">
                  <div>
                    <strong>AI Reference:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >with-stacked-flyout-menu</code
                    >
                  </div>
                  <div>
                    <strong>Component:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >WithStackedFlyoutMenu</code
                    >
                  </div>
                  <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
                </div>

                <div class="mt-4">
                  <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                  <div class="flex flex-wrap gap-1">
                    {
                      components
                        .find((c) => c.id === 'with-stacked-flyout-menu')
                        ?.tags.map((tag) => (
                          <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                        ))
                    }
                  </div>
                </div>
              </div>

              <div class="lg:col-span-2">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
                <pre
                  class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<WithStackedFlyoutMenu
  companyName="Your Company"
  logoSrc="/your-logo.svg"
  products={[
    { name: 'Analytics', description: 'Get insights', href: '#', icon: ChartPieIcon },
    { name: 'Security', description: 'Keep data safe', href: '#', icon: FingerPrintIcon }
  ]}
  navigation={[
    { name: 'Features', href: '#' },
    { name: 'Marketplace', href: '#' }
  ]}
/>`}</code>
              </pre>
              </div>
            </div>
          </div>
        </section>

        <!-- Constrained Component -->
        <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
          <!-- Component Preview -->
          <div class="border-b border-gray-200 dark:border-gray-700">
            <Constrained client:load />
          </div>

          <!-- Component Info -->
          <div class="p-6 bg-gray-50 dark:bg-gray-800">
            <div class="grid lg:grid-cols-3 gap-6">
              <div class="lg:col-span-1">
                <h3 class="text-xl font-semibold text-primary mb-2">Constrained</h3>
                <p class="text-muted mb-4">Simple constrained header with basic navigation and mobile menu</p>

                <div class="text-sm space-y-2">
                  <div>
                    <strong>AI Reference:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >constrained-header</code
                    >
                  </div>
                  <div>
                    <strong>Component:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">Constrained</code>
                  </div>
                  <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
                </div>

                <div class="mt-4">
                  <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                  <div class="flex flex-wrap gap-1">
                    {
                      components
                        .find((c) => c.id === 'constrained-header')
                        ?.tags.map((tag) => (
                          <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                        ))
                    }
                  </div>
                </div>
              </div>

              <div class="lg:col-span-2">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
                <pre
                  class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<Constrained
  companyName="Your Company"
  logoSrc="/your-logo.svg"
  navigation={[
    { name: 'Product', href: '#' },
    { name: 'Features', href: '#' },
    { name: 'Company', href: '#' }
  ]}
  loginHref="/login"
/>`}</code>
              </pre>
              </div>
            </div>
          </div>
        </section>

        <!-- On Brand Background Component -->
        <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
          <!-- Component Preview -->
          <div class="border-b border-gray-200 dark:border-gray-700">
            <OnBrandBackground client:load />
          </div>

          <!-- Component Info -->
          <div class="p-6 bg-gray-50 dark:bg-gray-800">
            <div class="grid lg:grid-cols-3 gap-6">
              <div class="lg:col-span-1">
                <h3 class="text-xl font-semibold text-primary mb-2">On Brand Background</h3>
                <p class="text-muted mb-4">Header with brand-colored background and white text elements</p>

                <div class="text-sm space-y-2">
                  <div>
                    <strong>AI Reference:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >on-brand-background-header</code
                    >
                  </div>
                  <div>
                    <strong>Component:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >OnBrandBackground</code
                    >
                  </div>
                  <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
                </div>

                <div class="mt-4">
                  <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                  <div class="flex flex-wrap gap-1">
                    {
                      components
                        .find((c) => c.id === 'on-brand-background-header')
                        ?.tags.map((tag) => (
                          <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                        ))
                    }
                  </div>
                </div>
              </div>

              <div class="lg:col-span-2">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
                <pre
                  class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<OnBrandBackground
  companyName="Your Company"
  logoSrc="/your-white-logo.svg"
  navigation={[
    { name: 'Product', href: '#' },
    { name: 'Features', href: '#' },
    { name: 'Company', href: '#' }
  ]}
  loginHref="/login"
  brandColor="indigo"
/>`}</code>
              </pre>
              </div>
            </div>
          </div>
        </section>

        <!-- With Full Width Flyout Menu Component -->
        <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
          <!-- Component Preview -->
          <div class="border-b border-gray-200 dark:border-gray-700">
            <WithFullWidthFlyoutMenu client:load />
          </div>

          <!-- Component Info -->
          <div class="p-6 bg-gray-50 dark:bg-gray-800">
            <div class="grid lg:grid-cols-3 gap-6">
              <div class="lg:col-span-1">
                <h3 class="text-xl font-semibold text-primary mb-2">With Full Width Flyout Menu</h3>
                <p class="text-muted mb-4">Navigation header with full-width flyout menu spanning the viewport</p>

                <div class="text-sm space-y-2">
                  <div>
                    <strong>AI Reference:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >with-full-width-flyout-menu</code
                    >
                  </div>
                  <div>
                    <strong>Component:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >WithFullWidthFlyoutMenu</code
                    >
                  </div>
                  <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
                </div>

                <div class="mt-4">
                  <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                  <div class="flex flex-wrap gap-1">
                    {
                      components
                        .find((c) => c.id === 'with-full-width-flyout-menu')
                        ?.tags.map((tag) => (
                          <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                        ))
                    }
                  </div>
                </div>
              </div>

              <div class="lg:col-span-2">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
                <pre
                  class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<WithFullWidthFlyoutMenu
  companyName="Your Company"
  logoSrc="/your-logo.svg"
  products={[
    { name: 'Analytics', description: 'Get insights', href: '#', icon: ChartPieIcon },
    { name: 'Security', description: 'Keep data safe', href: '#', icon: FingerPrintIcon }
  ]}
  callsToAction={[
    { name: 'Watch demo', href: '#', icon: PlayCircleIcon },
    { name: 'Contact sales', href: '#', icon: PhoneIcon }
  ]}
/>`}</code>
              </pre>
              </div>
            </div>
          </div>
        </section>

        <!-- Full Width Component -->
        <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
          <!-- Component Preview -->
          <div class="border-b border-gray-200 dark:border-gray-700">
            <FullWidth client:load />
          </div>

          <!-- Component Info -->
          <div class="p-6 bg-gray-50 dark:bg-gray-800">
            <div class="grid lg:grid-cols-3 gap-6">
              <div class="lg:col-span-1">
                <h3 class="text-xl font-semibold text-primary mb-2">Full Width</h3>
                <p class="text-muted mb-4">Full-width header without max-width container constraints</p>

                <div class="text-sm space-y-2">
                  <div>
                    <strong>AI Reference:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >full-width-header</code
                    >
                  </div>
                  <div>
                    <strong>Component:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">FullWidth</code>
                  </div>
                  <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
                </div>

                <div class="mt-4">
                  <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                  <div class="flex flex-wrap gap-1">
                    {
                      components
                        .find((c) => c.id === 'full-width-header')
                        ?.tags.map((tag) => (
                          <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                        ))
                    }
                  </div>
                </div>
              </div>

              <div class="lg:col-span-2">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
                <pre
                  class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<FullWidth
  companyName="Your Company"
  logoSrc="/your-logo.svg"
  navigation={[
    { name: 'Product', href: '#' },
    { name: 'Features', href: '#' },
    { name: 'Company', href: '#' }
  ]}
  loginHref="/login"
/>`}</code>
              </pre>
              </div>
            </div>
          </div>
        </section>

        <!-- With Call To Action Component -->
        <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
          <!-- Component Preview -->
          <div class="border-b border-gray-200 dark:border-gray-700">
            <WithCallToAction client:load />
          </div>

          <!-- Component Info -->
          <div class="p-6 bg-gray-50 dark:bg-gray-800">
            <div class="grid lg:grid-cols-3 gap-6">
              <div class="lg:col-span-1">
                <h3 class="text-xl font-semibold text-primary mb-2">With Call To Action</h3>
                <p class="text-muted mb-4">Header with prominent call-to-action button alongside navigation</p>

                <div class="text-sm space-y-2">
                  <div>
                    <strong>AI Reference:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >with-call-to-action-header</code
                    >
                  </div>
                  <div>
                    <strong>Component:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >WithCallToAction</code
                    >
                  </div>
                  <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
                </div>

                <div class="mt-4">
                  <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                  <div class="flex flex-wrap gap-1">
                    {
                      components
                        .find((c) => c.id === 'with-call-to-action-header')
                        ?.tags.map((tag) => (
                          <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                        ))
                    }
                  </div>
                </div>
              </div>

              <div class="lg:col-span-2">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
                <pre
                  class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<WithCallToAction
  companyName="Your Company"
  logoSrc="/your-logo.svg"
  navigation={[
    { name: 'Product', href: '#' },
    { name: 'Features', href: '#' },
    { name: 'Company', href: '#' }
  ]}
  loginHref="/login"
  ctaText="Sign up"
  ctaHref="/signup"
/>`}</code>
              </pre>
              </div>
            </div>
          </div>
        </section>

        <!-- With Multiple Flyout Menus Component -->
        <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
          <!-- Component Preview -->
          <div class="border-b border-gray-200 dark:border-gray-700">
            <WithMultipleFlyoutMenus client:load />
          </div>

          <!-- Component Info -->
          <div class="p-6 bg-gray-50 dark:bg-gray-800">
            <div class="grid lg:grid-cols-3 gap-6">
              <div class="lg:col-span-1">
                <h3 class="text-xl font-semibold text-primary mb-2">With Multiple Flyout Menus</h3>
                <p class="text-muted mb-4">
                  Header with multiple separate flyout menus for different navigation sections
                </p>

                <div class="text-sm space-y-2">
                  <div>
                    <strong>AI Reference:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >with-multiple-flyout-menus</code
                    >
                  </div>
                  <div>
                    <strong>Component:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >WithMultipleFlyoutMenus</code
                    >
                  </div>
                  <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
                </div>

                <div class="mt-4">
                  <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                  <div class="flex flex-wrap gap-1">
                    {
                      components
                        .find((c) => c.id === 'with-multiple-flyout-menus')
                        ?.tags.map((tag) => (
                          <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                        ))
                    }
                  </div>
                </div>
              </div>

              <div class="lg:col-span-2">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
                <pre
                  class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<WithMultipleFlyoutMenus
  companyName="Your Company"
  logoSrc="/your-logo.svg"
  products={[
    { name: 'Analytics', description: 'Get insights', href: '#', icon: ChartPieIcon },
    { name: 'Security', description: 'Keep data safe', href: '#', icon: FingerPrintIcon }
  ]}
  company={[
    { name: 'About us', href: '#' },
    { name: 'Careers', href: '#' }
  ]}
/>`}</code>
              </pre>
              </div>
            </div>
          </div>
        </section>

        <!-- With Icons In Mobile Menu Component -->
        <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
          <!-- Component Preview -->
          <div class="border-b border-gray-200 dark:border-gray-700">
            <WithIconsInMobileMenu client:load />
          </div>

          <!-- Component Info -->
          <div class="p-6 bg-gray-50 dark:bg-gray-800">
            <div class="grid lg:grid-cols-3 gap-6">
              <div class="lg:col-span-1">
                <h3 class="text-xl font-semibold text-primary mb-2">With Icons In Mobile Menu</h3>
                <p class="text-muted mb-4">Header with icons displayed in mobile menu alongside text navigation</p>

                <div class="text-sm space-y-2">
                  <div>
                    <strong>AI Reference:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >with-icons-in-mobile-menu</code
                    >
                  </div>
                  <div>
                    <strong>Component:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >WithIconsInMobileMenu</code
                    >
                  </div>
                  <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
                </div>

                <div class="mt-4">
                  <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                  <div class="flex flex-wrap gap-1">
                    {
                      components
                        .find((c) => c.id === 'with-icons-in-mobile-menu')
                        ?.tags.map((tag) => (
                          <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                        ))
                    }
                  </div>
                </div>
              </div>

              <div class="lg:col-span-2">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
                <pre
                  class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<WithIconsInMobileMenu
  companyName="Your Company"
  logoSrc="/your-logo.svg"
  products={[
    { name: 'Analytics', description: 'Get insights', href: '#', icon: ChartPieIcon },
    { name: 'Security', description: 'Keep data safe', href: '#', icon: FingerPrintIcon }
  ]}
  company={[
    { name: 'About us', href: '#', description: 'Company info' },
    { name: 'Careers', href: '#', description: 'Join our team' }
  ]}
/>`}</code>
              </pre>
              </div>
            </div>
          </div>
        </section>

        <!-- With Left Aligned Nav Component -->
        <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
          <!-- Component Preview -->
          <div class="border-b border-gray-200 dark:border-gray-700">
            <WithLeftAlignedNav client:load />
          </div>

          <!-- Component Info -->
          <div class="p-6 bg-gray-50 dark:bg-gray-800">
            <div class="grid lg:grid-cols-3 gap-6">
              <div class="lg:col-span-1">
                <h3 class="text-xl font-semibold text-primary mb-2">With Left Aligned Nav</h3>
                <p class="text-muted mb-4">Header with navigation items aligned to the left alongside the logo</p>

                <div class="text-sm space-y-2">
                  <div>
                    <strong>AI Reference:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >with-left-aligned-nav</code
                    >
                  </div>
                  <div>
                    <strong>Component:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >WithLeftAlignedNav</code
                    >
                  </div>
                  <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
                </div>

                <div class="mt-4">
                  <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                  <div class="flex flex-wrap gap-1">
                    {
                      components
                        .find((c) => c.id === 'with-left-aligned-nav')
                        ?.tags.map((tag) => (
                          <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                        ))
                    }
                  </div>
                </div>
              </div>

              <div class="lg:col-span-2">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
                <pre
                  class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<WithLeftAlignedNav
  companyName="Your Company"
  logoSrc="/your-logo.svg"
  navigation={[
    { name: 'Product', href: '#' },
    { name: 'Features', href: '#' },
    { name: 'Company', href: '#' }
  ]}
  loginHref="/login"
/>`}</code>
              </pre>
              </div>
            </div>
          </div>
        </section>

        <!-- With Right Aligned Nav Component -->
        <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
          <!-- Component Preview -->
          <div class="border-b border-gray-200 dark:border-gray-700">
            <WithRightAlignedNav client:load />
          </div>

          <!-- Component Info -->
          <div class="p-6 bg-gray-50 dark:bg-gray-800">
            <div class="grid lg:grid-cols-3 gap-6">
              <div class="lg:col-span-1">
                <h3 class="text-xl font-semibold text-primary mb-2">With Right Aligned Nav</h3>
                <p class="text-muted mb-4">Header with navigation items and login aligned to the right side</p>

                <div class="text-sm space-y-2">
                  <div>
                    <strong>AI Reference:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >with-right-aligned-nav</code
                    >
                  </div>
                  <div>
                    <strong>Component:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >WithRightAlignedNav</code
                    >
                  </div>
                  <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
                </div>

                <div class="mt-4">
                  <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                  <div class="flex flex-wrap gap-1">
                    {
                      components
                        .find((c) => c.id === 'with-right-aligned-nav')
                        ?.tags.map((tag) => (
                          <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                        ))
                    }
                  </div>
                </div>
              </div>

              <div class="lg:col-span-2">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
                <pre
                  class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<WithRightAlignedNav
  companyName="Your Company"
  logoSrc="/your-logo.svg"
  navigation={[
    { name: 'Product', href: '#' },
    { name: 'Features', href: '#' },
    { name: 'Company', href: '#' }
  ]}
  loginHref="/login"
/>`}</code>
              </pre>
              </div>
            </div>
          </div>
        </section>

        <!-- With Centered Logo Component -->
        <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
          <!-- Component Preview -->
          <div class="border-b border-gray-200 dark:border-gray-700">
            <WithCenteredLogo client:load />
          </div>

          <!-- Component Info -->
          <div class="p-6 bg-gray-50 dark:bg-gray-800">
            <div class="grid lg:grid-cols-3 gap-6">
              <div class="lg:col-span-1">
                <h3 class="text-xl font-semibold text-primary mb-2">With Centered Logo</h3>
                <p class="text-muted mb-4">Header with centered logo and navigation split on both sides</p>

                <div class="text-sm space-y-2">
                  <div>
                    <strong>AI Reference:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >with-centered-logo</code
                    >
                  </div>
                  <div>
                    <strong>Component:</strong>
                    <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                      >WithCenteredLogo</code
                    >
                  </div>
                  <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
                </div>

                <div class="mt-4">
                  <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                  <div class="flex flex-wrap gap-1">
                    {
                      components
                        .find((c) => c.id === 'with-centered-logo')
                        ?.tags.map((tag) => (
                          <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                        ))
                    }
                  </div>
                </div>
              </div>

              <div class="lg:col-span-2">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
                <pre
                  class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<WithCenteredLogo
  companyName="Your Company"
  logoSrc="/your-logo.svg"
  navigation={[
    { name: 'Product', href: '#' },
    { name: 'Features', href: '#' },
    { name: 'Company', href: '#' }
  ]}
  loginHref="/login"
/>`}</code>
              </pre>
              </div>
            </div>
          </div>
        </section>
      </div>

      <!-- Navigation -->
      <div class="mt-12 flex justify-between items-center">
        <a href="/dev-components" class="inline-flex items-center text-primary hover:text-primary/80 font-medium">
          ← Back to Component Library
        </a>
        <a href="/styles-guide" class="inline-flex items-center text-muted hover:text-primary font-medium">
          Styles Guide →
        </a>
      </div>
    </div>
  </DevComponentsSidebar>
</Layout>
