---
import Layout from '~/layouts/PageLayout.astro';
import ContactFormWithGradient from '~/components/ui/page-sections/contact-sections/ContactFormWithGradient.tsx';
import SideBySideGrid from '~/components/ui/page-sections/contact-sections/SideBySideGrid.tsx';
import SplitWithPattern from '~/components/ui/page-sections/contact-sections/SplitWithPattern.tsx';
import SimpleFourColumn from '~/components/ui/page-sections/contact-sections/SimpleFourColumn.tsx';
import SimpleCentered from '~/components/ui/page-sections/contact-sections/SimpleCentered.tsx';
import WithTestimonial from '~/components/ui/page-sections/contact-sections/WithTestimonial.tsx';
import SplitWithImage from '~/components/ui/page-sections/contact-sections/SplitWithImage.tsx';
import { getComponentsBySubcategory } from '~/lib/component-registry';

const metadata = {
  title: 'Contact Sections — Component Library',
  description: 'Collection of contact section components for development reference.',
  robots: { index: false, follow: false },
};

// Get components for this subcategory
const components = getComponentsBySubcategory('contact-sections');
---

<Layout metadata={metadata}>
  <div class="container mx-auto px-4 py-8 max-w-7xl">

    <!-- Developer Warning Banner -->
    <div class="bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 rounded-lg p-4 mb-8">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M8.485 2.495c.673-1.167 2.357-1.167 3.03 0l6.28 10.875c.673 1.167-.17 2.625-1.516 2.625H3.72c-1.347 0-2.19-1.458-1.515-2.625L8.485 2.495zM10 5a.75.75 0 01.75.75v3.5a.75.75 0 01-1.5 0v-3.5A.75.75 0 0110 5zm0 9a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd" />
          </svg>
        </div>
        <div class="ml-3">
          <h3 class="text-sm font-medium text-red-800 dark:text-red-200">
            Development Component Library
          </h3>
          <div class="mt-2 text-sm text-red-700 dark:text-red-300">
            <p>This page is for development purposes only. Components use Tailwind UI Plus license. Do not expose to public.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Header -->
    <div class="mb-8">
      <div class="flex items-center gap-2 text-sm text-muted mb-2">
        <a href="/dev-components" class="hover:text-primary">Component Library</a>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
        </svg>
        <span>Contact Sections</span>
      </div>
      <h1 class="text-4xl font-bold mb-4">Contact Sections</h1>
      <p class="text-lg text-muted max-w-3xl">
        Contact forms and contact information sections with clear AI naming conventions. Perfect for creating effective contact experiences.
      </p>
    </div>

    <!-- Component Stats -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-8">
      <div class="bg-white dark:bg-gray-900 rounded-lg border p-4">
        <div class="text-2xl font-bold text-primary mb-1">7</div>
        <div class="text-sm text-muted">Components</div>
      </div>
      <div class="bg-white dark:bg-gray-900 rounded-lg border p-4">
        <div class="text-2xl font-bold text-primary mb-1">100%</div>
        <div class="text-sm text-muted">Responsive</div>
      </div>
      <div class="bg-white dark:bg-gray-900 rounded-lg border p-4">
        <div class="text-2xl font-bold text-primary mb-1">100%</div>
        <div class="text-sm text-muted">Dark Mode</div>
      </div>
      <div class="bg-white dark:bg-gray-900 rounded-lg border p-4">
        <div class="text-2xl font-bold text-primary mb-1">AI Ready</div>
        <div class="text-sm text-muted">Optimized</div>
      </div>
    </div>

    <!-- Component Library -->
    <div class="space-y-16">

      <!-- Contact Form With Gradient Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <ContactFormWithGradient client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Contact Form With Gradient</h3>
              <p class="text-muted mb-4">Full contact form with gradient background, phone input with country selector, and privacy toggle</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">contact-form-with-gradient</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">ContactFormWithGradient</code></div>
                <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'contact-form-with-gradient')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<ContactFormWithGradient
  title="Contact sales"
  subtitle="Aute magna irure deserunt veniam aliqua magna enim voluptate."
  buttonText="Let's talk"
/>`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Side By Side Grid Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <SideBySideGrid client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Side By Side Grid</h3>
              <p class="text-muted mb-4">Contact information and locations displayed in grid cards with dual sections</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">side-by-side-grid</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">SideBySideGrid</code></div>
                <div><strong>Complexity:</strong> <span class="text-yellow-600">Medium</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'side-by-side-grid')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<SideBySideGrid
  contactTitle="Get in touch"
  contactSubtitle="Quam nunc nunc eu sed. Sed rhoncus quis ultricies ac pellentesque."
  locationsTitle="Locations"
  locationsSubtitle="Consequat sunt cillum cillum elit sint. Qui occaecat nisi in ipsum commodo."
/>`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Split With Pattern Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <SplitWithPattern client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Split With Pattern</h3>
              <p class="text-muted mb-4">Split layout with patterned background, contact info with icons, and contact form</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">split-with-pattern</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">SplitWithPattern</code></div>
                <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'split-with-pattern')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<SplitWithPattern
  title="Get in touch"
  subtitle="Proin volutpat consequat porttitor cras nullam gravida at. Orci molestie a eu arcu."
  address="545 Mavis Island, Chicago, IL 99191"
  phone="+1 (555) 234-5678"
  email="hello@example.com"
/>`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Simple Four Column Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <SimpleFourColumn client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Simple Four Column</h3>
              <p class="text-muted mb-4">Four-column office locations layout with left-aligned borders and address information</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">simple-four-column</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">SimpleFourColumn</code></div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'simple-four-column')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<SimpleFourColumn
  title="Our offices"
  subtitle="Varius facilisi mauris sed sit. Non sed et duis dui leo, vulputate id malesuada non."
  offices={[
    { name: 'Los Angeles', address: '4556 Brendan Ferry\\nLos Angeles, CA 90210' },
    { name: 'New York', address: '886 Walter Street\\nNew York, NY 12345' }
  ]}
/>`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Simple Centered Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <SimpleCentered client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Simple Centered</h3>
              <p class="text-muted mb-4">Centered contact options with icons, support categories, and call-to-action links</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">simple-centered</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">SimpleCentered</code></div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'simple-centered')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<SimpleCentered
  title="Contact sales"
  subtitle="Aute magna irure deserunt veniam aliqua magna enim voluptate."
  supportOptions={[
    { icon: ChatBubbleLeftRightIcon, title: 'Sales support', description: 'Ut cursus est ut amet...', link: 'Contact us' },
    { icon: BugAntIcon, title: 'Bug reports', description: 'Expedita qui non ut quia...', link: 'Report a bug' }
  ]}
/>`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- With Testimonial Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <WithTestimonial client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">With Testimonial</h3>
              <p class="text-muted mb-4">Contact form with SVG pattern background, testimonial quote, and company logo</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">with-testimonial</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">WithTestimonial</code></div>
                <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'with-testimonial')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<WithTestimonial
  title="Let's talk about your project"
  subtitle="We help companies and individuals build out their brand guidelines."
  testimonial={{
    quote: "Lorem ipsum dolor sit amet consectetur adipisicing elit...",
    author: "Brenna Goyette",
    role: "CEO of Workcation",
    avatar: "avatar-url"
  }}
/>`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Split With Image Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <SplitWithImage client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Split With Image</h3>
              <p class="text-muted mb-4">Split layout with full-height image and comprehensive form with budget radio buttons</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">split-with-image</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">SplitWithImage</code></div>
                <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'split-with-image')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<SplitWithImage
  title="Let's work together"
  subtitle="Proin volutpat consequat porttitor cras nullam gravida at orci molestie a eu arcu."
  backgroundImage="your-background-image-url"
  budgetOptions={[
    { value: 'under_25k', label: 'Less than $25K' },
    { value: '25k-50k', label: '$25K – $50K' }
  ]}
/>`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

    </div>

    <!-- Navigation -->
    <div class="mt-12 flex justify-between items-center">
      <a
        href="/dev-components"
        class="inline-flex items-center text-primary hover:text-primary/80 font-medium"
      >
        ← Back to Component Library
      </a>
      <a
        href="/styles-guide"
        class="inline-flex items-center text-muted hover:text-primary font-medium"
      >
        Styles Guide →
      </a>
    </div>

  </div>
</Layout>