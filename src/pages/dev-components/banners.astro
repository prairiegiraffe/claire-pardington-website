---
import Layout from '~/layouts/PageLayout.astro';
import ButtonBanner from '~/components/ui/page-sections/banners/ButtonBanner';
import DarkBanner from '~/components/ui/page-sections/banners/DarkBanner';
import BrandBanner from '~/components/ui/page-sections/banners/BrandBanner';
import BackgroundGlowBanner from '~/components/ui/page-sections/banners/BackgroundGlowBanner';
import LinkBanner from '~/components/ui/page-sections/banners/LinkBanner';
import LeftAlignedBanner from '~/components/ui/page-sections/banners/LeftAlignedBanner';
import BottomAlignedBanner from '~/components/ui/page-sections/banners/BottomAlignedBanner';
import FloatingBottomBanner from '~/components/ui/page-sections/banners/FloatingBottomBanner';
import FloatingCenteredBanner from '~/components/ui/page-sections/banners/FloatingCenteredBanner';
import PrivacyRightAlignedBanner from '~/components/ui/page-sections/banners/PrivacyRightAlignedBanner';
import PrivacyCenteredBanner from '~/components/ui/page-sections/banners/PrivacyCenteredBanner';
import PrivacyLeftAlignedBanner from '~/components/ui/page-sections/banners/PrivacyLeftAlignedBanner';
import PrivacyFullWidthBanner from '~/components/ui/page-sections/banners/PrivacyFullWidthBanner';
import { getComponentsBySubcategory } from '~/lib/component-registry';

const metadata = {
  title: 'Banner Components — Component Library',
  description: 'Collection of banner components for development reference.',
  robots: { index: false, follow: false },
};

// Get components for this subcategory
const components = getComponentsBySubcategory('banners');
---

<Layout metadata={metadata}>
  <div class="container mx-auto px-4 py-8 max-w-7xl">

    <!-- Developer Warning Banner -->
    <div class="bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 rounded-lg p-4 mb-8">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M8.485 2.495c.673-1.167 2.357-1.167 3.03 0l6.28 10.875c.673 1.167-.17 2.625-1.516 2.625H3.72c-1.347 0-2.19-1.458-1.515-2.625L8.485 2.495zM10 5a.75.75 0 01.75.75v3.5a.75.75 0 01-1.5 0v-3.5A.75.75 0 0110 5zm0 9a1 1 0 100-2 1 1 0 000 2z" clip-rule="evenodd" />
          </svg>
        </div>
        <div class="ml-3">
          <h3 class="text-sm font-medium text-red-800 dark:text-red-200">
            Development Component Library
          </h3>
          <div class="mt-2 text-sm text-red-700 dark:text-red-300">
            <p>This page is for development purposes only. Components use Tailwind UI Plus license. Do not expose to public.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Header -->
    <div class="mb-8">
      <div class="flex items-center gap-2 text-sm text-muted mb-2">
        <a href="/dev-components" class="hover:text-primary">Component Library</a>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
        </svg>
        <span>Banners</span>
      </div>
      <h1 class="text-4xl font-bold mb-4">Banner Components</h1>
      <p class="text-lg text-muted max-w-3xl">
        Notification and announcement banners with clear AI naming conventions. Perfect for user communications and privacy notices.
      </p>
    </div>

    <!-- Component Stats -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-8">
      <div class="bg-white dark:bg-gray-900 rounded-lg border p-4">
        <div class="text-2xl font-bold text-primary mb-1">14</div>
        <div class="text-sm text-muted">Components</div>
      </div>
      <div class="bg-white dark:bg-gray-900 rounded-lg border p-4">
        <div class="text-2xl font-bold text-primary mb-1">100%</div>
        <div class="text-sm text-muted">Responsive</div>
      </div>
      <div class="bg-white dark:bg-gray-900 rounded-lg border p-4">
        <div class="text-2xl font-bold text-primary mb-1">100%</div>
        <div class="text-sm text-muted">Dark Mode</div>
      </div>
      <div class="bg-white dark:bg-gray-900 rounded-lg border p-4">
        <div class="text-2xl font-bold text-primary mb-1">AI Ready</div>
        <div class="text-sm text-muted">Optimized</div>
      </div>
    </div>

    <!-- Component Library -->
    <div class="space-y-16">

      <!-- Button Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <ButtonBanner client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Banner with Button</h3>
              <p class="text-muted mb-4">Notification banner with gradient background, message text, and call-to-action button</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">button-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">ButtonBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'button-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<ButtonBanner />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Dark Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <DarkBanner client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Dark Banner</h3>
              <p class="text-muted mb-4">Simple dark banner with white text and dismiss button</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">dark-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">DarkBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'dark-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<DarkBanner />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Brand Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <BrandBanner client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Brand Banner</h3>
              <p class="text-muted mb-4">Banner with brand color background and announcement text</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">brand-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">BrandBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'brand-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<BrandBanner />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Background Glow Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <BackgroundGlowBanner client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Background Glow Banner</h3>
              <p class="text-muted mb-4">Banner with gradient background effects and glow elements</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">background-glow-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">BackgroundGlowBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-yellow-600">Medium</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'background-glow-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<BackgroundGlowBanner />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Link Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <LinkBanner client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Link Banner</h3>
              <p class="text-muted mb-4">Banner with inline text link and gradient background</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">link-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">LinkBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'link-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<LinkBanner />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Left Aligned Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <LeftAlignedBanner client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Left Aligned Banner</h3>
              <p class="text-muted mb-4">Left-aligned banner with dark background and close button on right</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">left-aligned-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">LeftAlignedBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'left-aligned-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<LeftAlignedBanner />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Bottom Aligned Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <div class="relative h-40 bg-gray-100 dark:bg-gray-900 flex items-center justify-center overflow-hidden">
            <p class="text-sm text-muted">Preview shows positioned at bottom of container</p>
            <div class="absolute inset-x-0 bottom-0">
              <div class="flex items-center gap-x-6 bg-gray-900 px-6 py-2.5 sm:px-3.5 sm:before:flex-1 dark:bg-gray-800 dark:after:pointer-events-none dark:after:absolute dark:after:inset-x-0 dark:after:top-0 dark:after:h-px dark:after:bg-white/10">
                <p class="text-sm/6 text-white">
                  <a href="#">
                    <strong class="font-semibold">GeneriCon 2023</strong>
                    <svg viewBox="0 0 2 2" aria-hidden="true" class="mx-2 inline size-0.5 fill-current">
                      <circle r={1} cx={1} cy={1} />
                    </svg>
                    Join us in Denver from June 7 – 9 to see what's coming next&nbsp;<span aria-hidden="true">&rarr;</span>
                  </a>
                </p>
                <div class="flex flex-1 justify-end">
                  <button type="button" class="-m-3 p-3 focus-visible:-outline-offset-4">
                    <span class="sr-only">Dismiss</span>
                    <svg aria-hidden="true" class="size-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M6.28 5.22a.75.75 0 00-1.06 1.06L8.94 10l-3.72 3.72a.75.75 0 101.06 1.06L10 11.06l3.72 3.72a.75.75 0 101.06-1.06L11.06 10l3.72-3.72a.75.75 0 00-1.06-1.06L10 8.94 6.28 5.22z" />
                    </svg>
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Bottom Aligned Banner</h3>
              <p class="text-muted mb-4">Fixed banner positioned at bottom of screen with full width</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">bottom-aligned-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">BottomAlignedBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-yellow-600">Medium</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'bottom-aligned-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<BottomAlignedBanner />
{/* Note: Add bottom padding to pages to prevent content from being obscured */}`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Floating Bottom Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <div class="relative h-40 bg-gray-100 dark:bg-gray-900 flex items-center justify-center overflow-hidden">
            <p class="text-sm text-muted">Preview shows floating at bottom with margins</p>
            <div class="absolute inset-x-0 bottom-0 px-6 pb-5">
              <div class="flex items-center justify-between gap-x-6 bg-gray-900 px-6 py-2.5 rounded-xl py-3 pr-3.5 pl-4 dark:bg-gray-800 dark:ring-1 dark:ring-white/10">
                <p class="text-sm/6 text-white">
                  <a href="#">
                    <strong class="font-semibold">GeneriCon 2023</strong>
                    <svg viewBox="0 0 2 2" aria-hidden="true" class="mx-2 inline size-0.5 fill-current">
                      <circle r={1} cx={1} cy={1} />
                    </svg>
                    Join us in Denver from June 7 – 9 to see what's coming next&nbsp;<span aria-hidden="true">&rarr;</span>
                  </a>
                </p>
                <button type="button" class="-m-3 flex-none p-3 focus-visible:-outline-offset-4">
                  <span class="sr-only">Dismiss</span>
                  <svg aria-hidden="true" class="size-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M6.28 5.22a.75.75 0 00-1.06 1.06L8.94 10l-3.72 3.72a.75.75 0 101.06 1.06L10 11.06l3.72 3.72a.75.75 0 101.06-1.06L11.06 10l3.72-3.72a.75.75 0 00-1.06-1.06L10 8.94 6.28 5.22z" />
                  </svg>
                </button>
              </div>
            </div>
          </div>
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Floating Bottom Banner</h3>
              <p class="text-muted mb-4">Floating banner positioned at bottom with rounded corners</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">floating-bottom-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">FloatingBottomBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-yellow-600">Medium</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'floating-bottom-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<FloatingBottomBanner />
{/* Note: Add bottom padding to pages to prevent content from being obscured */}`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Floating Centered Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <div class="relative h-40 bg-gray-100 dark:bg-gray-900 flex items-center justify-center overflow-hidden">
            <p class="text-sm text-muted">Preview shows centered floating at bottom</p>
            <div class="absolute inset-x-0 bottom-0 flex justify-center px-6 pb-5">
              <div class="flex items-center justify-between gap-x-6 bg-gray-900 px-6 py-2.5 rounded-xl py-3 pr-3.5 pl-4 dark:bg-gray-800 dark:ring-1 dark:ring-white/10">
                <p class="text-sm/6 text-white">
                  <a href="#">
                    <strong class="font-semibold">GeneriCon 2023</strong>
                    <svg viewBox="0 0 2 2" aria-hidden="true" class="mx-2 inline size-0.5 fill-current">
                      <circle r={1} cx={1} cy={1} />
                    </svg>
                    Join us in Denver from June 7 – 9 to see what's coming next&nbsp;<span aria-hidden="true">&rarr;</span>
                  </a>
                </p>
                <button type="button" class="-m-1.5 flex-none p-1.5">
                  <span class="sr-only">Dismiss</span>
                  <svg aria-hidden="true" class="size-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M6.28 5.22a.75.75 0 00-1.06 1.06L8.94 10l-3.72 3.72a.75.75 0 101.06 1.06L10 11.06l3.72 3.72a.75.75 0 101.06-1.06L11.06 10l3.72-3.72a.75.75 0 00-1.06-1.06L10 8.94 6.28 5.22z" />
                  </svg>
                </button>
              </div>
            </div>
          </div>
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Floating Centered Banner</h3>
              <p class="text-muted mb-4">Centered floating banner at bottom with rounded design</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">floating-centered-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">FloatingCenteredBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-yellow-600">Medium</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'floating-centered-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<FloatingCenteredBanner />
{/* Note: Add bottom padding to pages to prevent content from being obscured */}`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Privacy Right Aligned Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <div class="relative h-48 bg-gray-100 dark:bg-gray-900 flex items-center justify-center overflow-hidden">
            <p class="text-sm text-muted">Preview shows privacy notice positioned right</p>
            <div class="absolute inset-x-0 bottom-0 px-6 pb-6">
              <div class="ml-auto max-w-xl rounded-xl bg-white p-6 shadow-lg outline-1 outline-gray-900/10 dark:bg-gray-800 dark:shadow-none dark:-outline-offset-1 dark:outline-white/10">
                <p class="text-sm/6 text-gray-900 dark:text-white">
                  This website uses cookies to supplement a balanced diet and provide a much deserved reward to the senses after
                  consuming bland but nutritious meals. Accepting our cookies is optional but recommended, as they are
                  delicious. See our{' '}
                  <a
                    href="#"
                    class="font-semibold text-indigo-600 hover:text-indigo-500 dark:text-indigo-400 dark:hover:text-indigo-300"
                  >
                    cookie policy
                  </a>
                  .
                </p>
                <div class="mt-4 flex items-center gap-x-5">
                  <button
                    type="button"
                    class="rounded-md bg-gray-900 px-3 py-2 text-sm font-semibold text-white shadow-xs hover:bg-gray-700 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gray-900 dark:bg-gray-700 dark:ring-1 dark:ring-white/10 dark:hover:bg-white/15 dark:focus-visible:outline-white"
                  >
                    Accept all
                  </button>
                  <button
                    type="button"
                    class="text-sm/6 font-semibold text-gray-900 hover:text-gray-700 dark:text-white dark:hover:text-gray-300"
                  >
                    Reject all
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Privacy Notice Right Aligned</h3>
              <p class="text-muted mb-4">Cookie consent banner with right alignment and action buttons</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">privacy-right-aligned-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">PrivacyRightAlignedBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'privacy-right-aligned-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<PrivacyRightAlignedBanner />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Privacy Centered Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <div class="relative h-48 bg-gray-100 dark:bg-gray-900 flex items-center justify-center overflow-hidden">
            <p class="text-sm text-muted">Preview shows privacy notice centered</p>
            <div class="absolute inset-x-0 bottom-0 px-6 pb-6">
              <div class="mx-auto max-w-xl rounded-xl bg-white p-6 shadow-lg outline-1 outline-gray-900/10 dark:bg-gray-800 dark:shadow-none dark:-outline-offset-1 dark:outline-white/10">
                <p class="text-sm/6 text-gray-900 dark:text-white">
                  This website uses cookies to supplement a balanced diet and provide a much deserved reward to the senses after
                  consuming bland but nutritious meals. Accepting our cookies is optional but recommended, as they are
                  delicious. See our{' '}
                  <a
                    href="#"
                    class="font-semibold text-indigo-600 hover:text-indigo-500 dark:text-indigo-400 dark:hover:text-indigo-300"
                  >
                    cookie policy
                  </a>
                  .
                </p>
                <div class="mt-4 flex items-center gap-x-5">
                  <button
                    type="button"
                    class="rounded-md bg-gray-900 px-3 py-2 text-sm font-semibold text-white shadow-xs hover:bg-gray-700 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gray-900 dark:bg-gray-700 dark:ring-1 dark:ring-white/10 dark:hover:bg-white/15 dark:focus-visible:outline-white"
                  >
                    Accept all
                  </button>
                  <button
                    type="button"
                    class="text-sm/6 font-semibold text-gray-900 hover:text-gray-700 dark:text-white dark:hover:text-gray-300"
                  >
                    Reject all
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Privacy Notice Centered</h3>
              <p class="text-muted mb-4">Cookie consent banner with centered layout and action buttons</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">privacy-centered-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">PrivacyCenteredBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'privacy-centered-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<PrivacyCenteredBanner />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Privacy Left Aligned Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <div class="relative h-48 bg-gray-100 dark:bg-gray-900 flex items-center justify-center overflow-hidden">
            <p class="text-sm text-muted">Preview shows privacy notice positioned left</p>
            <div class="absolute inset-x-0 bottom-0 px-6 pb-6">
              <div class="max-w-xl rounded-xl bg-white p-6 shadow-lg outline-1 outline-gray-900/10 dark:bg-gray-800 dark:shadow-none dark:-outline-offset-1 dark:outline-white/10">
                <p class="text-sm/6 text-gray-900 dark:text-white">
                  This website uses cookies to supplement a balanced diet and provide a much deserved reward to the senses after
                  consuming bland but nutritious meals. Accepting our cookies is optional but recommended, as they are
                  delicious. See our{' '}
                  <a
                    href="#"
                    class="font-semibold text-indigo-600 hover:text-indigo-500 dark:text-indigo-400 dark:hover:text-indigo-300"
                  >
                    cookie policy
                  </a>
                  .
                </p>
                <div class="mt-4 flex items-center gap-x-5">
                  <button
                    type="button"
                    class="rounded-md bg-gray-900 px-3 py-2 text-sm font-semibold text-white shadow-xs hover:bg-gray-700 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gray-900 dark:bg-gray-700 dark:ring-1 dark:ring-white/10 dark:hover:bg-white/15 dark:focus-visible:outline-white"
                  >
                    Accept all
                  </button>
                  <button
                    type="button"
                    class="text-sm/6 font-semibold text-gray-900 hover:text-gray-700 dark:text-white dark:hover:text-gray-300"
                  >
                    Reject all
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Privacy Notice Left Aligned</h3>
              <p class="text-muted mb-4">Cookie consent banner with left alignment and action buttons</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">privacy-left-aligned-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">PrivacyLeftAlignedBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'privacy-left-aligned-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<PrivacyLeftAlignedBanner />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Privacy Full Width Banner Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <div class="relative h-48 bg-gray-100 dark:bg-gray-900 flex items-center justify-center overflow-hidden">
            <p class="text-sm text-muted">Preview shows full-width privacy notice at bottom</p>
            <div class="absolute inset-x-0 bottom-0">
              <div class="flex flex-col justify-between gap-x-8 gap-y-4 border-t border-gray-900/10 bg-white p-6 shadow-lg md:flex-row md:items-center lg:px-8 dark:border-white/10 dark:bg-gray-800 dark:shadow-none">
                <p class="max-w-4xl text-sm/6 text-gray-900 dark:text-white">
                  This website uses cookies to supplement a balanced diet and provide a much deserved reward to the senses after
                  consuming bland but nutritious meals. Accepting our cookies is optional but recommended, as they are delicious.
                  See our{' '}
                  <a
                    href="#"
                    class="font-semibold text-indigo-600 hover:text-indigo-500 dark:text-indigo-400 dark:hover:text-indigo-300"
                  >
                    cookie policy
                  </a>
                  .
                </p>
                <div class="flex flex-none items-center gap-x-5">
                  <button
                    type="button"
                    class="rounded-md bg-gray-900 px-3 py-2 text-sm font-semibold text-white shadow-xs hover:bg-gray-700 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gray-900 dark:bg-gray-700 dark:ring-1 dark:ring-white/10 dark:hover:bg-white/15 dark:focus-visible:outline-white"
                  >
                    Accept all
                  </button>
                  <button
                    type="button"
                    class="text-sm/6 font-semibold text-gray-900 hover:text-gray-700 dark:text-gray-300 dark:hover:text-white"
                  >
                    Reject all
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Privacy Notice Full Width</h3>
              <p class="text-muted mb-4">Full-width cookie consent banner with action buttons</p>

              <div class="text-sm space-y-2">
                <div><strong>AI Reference:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">privacy-full-width-banner</code></div>
                <div><strong>Component:</strong> <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">PrivacyFullWidthBanner</code></div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  {components.find(c => c.id === 'privacy-full-width-banner')?.tags.map(tag => (
                    <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">{tag}</span>
                  ))}
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<PrivacyFullWidthBanner />
{/* Note: Add bottom padding to pages to prevent content from being obscured */}`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

    </div>

    <!-- Navigation -->
    <div class="mt-12 flex justify-between items-center">
      <a
        href="/dev-components"
        class="inline-flex items-center text-primary hover:text-primary/80 font-medium"
      >
        ← Back to Component Library
      </a>
      <a
        href="/styles-guide"
        class="inline-flex items-center text-muted hover:text-primary font-medium"
      >
        Styles Guide →
      </a>
    </div>

  </div>
</Layout>