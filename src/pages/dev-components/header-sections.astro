---
import Layout from '~/layouts/Layout.astro';
import DevComponentsSidebar from '~/components/ui/DevComponentsSidebar.astro';
import HeaderWithStats from '~/components/ui/page-sections/header-sections/HeaderWithStats';
import CenteredHeader from '~/components/ui/page-sections/header-sections/CenteredHeader';
import CenteredWithEyebrowHeader from '~/components/ui/page-sections/header-sections/CenteredWithEyebrowHeader';
import HeaderWithCards from '~/components/ui/page-sections/header-sections/HeaderWithCards';
import SimpleHeader from '~/components/ui/page-sections/header-sections/SimpleHeader';
import SimpleWithEyebrowHeader from '~/components/ui/page-sections/header-sections/SimpleWithEyebrowHeader';
import SimpleWithBackgroundImageHeader from '~/components/ui/page-sections/header-sections/SimpleWithBackgroundImageHeader';
import CenteredWithBackgroundImageHeader from '~/components/ui/page-sections/header-sections/CenteredWithBackgroundImageHeader';
import { getComponentsBySubcategory } from '~/lib/component-registry';

const metadata = {
  title: 'Header Sections — Component Library',
  description: 'Collection of header section components for development reference.',
  robots: { index: false, follow: false },
};

// Get components for this subcategory
const components = getComponentsBySubcategory('header-sections');
---

<Layout metadata={metadata}>
<DevComponentsSidebar>
  <div class="container mx-auto px-4 py-8 max-w-7xl">
    <!-- Header -->
    <div class="mb-8">
      <div class="flex items-center gap-2 text-sm text-muted mb-2">
        <a href="/dev-components" class="hover:text-primary">Component Library</a>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
        </svg>
        <span>Header Sections</span>
      </div>
      <h1 class="text-4xl font-bold mb-4">Header Sections</h1>
      <p class="text-lg text-muted max-w-3xl">
        Professional page headers for different content types, support sections, and landing pages.
      </p>
    </div>

    <!-- Component Library -->
    <div class="space-y-16">
      <!-- Header With Stats Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <HeaderWithStats client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Header With Stats</h3>
              <p class="text-muted mb-4">Header with background image, navigation links, and statistics grid</p>

              <div class="text-sm space-y-2">
                <div>
                  <strong>AI Reference:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">header-with-stats</code
                  >
                </div>
                <div>
                  <strong>Component:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">HeaderWithStats</code>
                </div>
                <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">header</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">stats</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">background-image</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">gradient</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">links</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">careers</span>
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre
                class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<HeaderWithStats />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Centered Header Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <CenteredHeader client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Centered Header</h3>
              <p class="text-muted mb-4">Simple centered header with title and description</p>

              <div class="text-sm space-y-2">
                <div>
                  <strong>AI Reference:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">centered-header</code>
                </div>
                <div>
                  <strong>Component:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">CenteredHeader</code>
                </div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">header</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">centered</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">simple</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">minimal</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">clean</span>
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre
                class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<CenteredHeader />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Centered With Eyebrow Header Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <CenteredWithEyebrowHeader client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Centered With Eyebrow Header</h3>
              <p class="text-muted mb-4">Centered header with eyebrow text, title, and description</p>

              <div class="text-sm space-y-2">
                <div>
                  <strong>AI Reference:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >centered-with-eyebrow-header</code
                  >
                </div>
                <div>
                  <strong>Component:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >CenteredWithEyebrowHeader</code
                  >
                </div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">header</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">centered</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">eyebrow</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">branded</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">support</span>
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre
                class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<CenteredWithEyebrowHeader />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Header With Cards Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <HeaderWithCards client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Header With Cards</h3>
              <p class="text-muted mb-4">Header with background effects and support category cards with icons</p>

              <div class="text-sm space-y-2">
                <div>
                  <strong>AI Reference:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">header-with-cards</code
                  >
                </div>
                <div>
                  <strong>Component:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">HeaderWithCards</code>
                </div>
                <div><strong>Complexity:</strong> <span class="text-red-600">Complex</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">header</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">cards</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">icons</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">background-image</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">gradient</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">support</span>
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre
                class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<HeaderWithCards />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Simple Header Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <SimpleHeader client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Simple Header</h3>
              <p class="text-muted mb-4">Left-aligned basic header with title and description</p>

              <div class="text-sm space-y-2">
                <div>
                  <strong>AI Reference:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">simple-header</code>
                </div>
                <div>
                  <strong>Component:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs">SimpleHeader</code>
                </div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">header</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">left-aligned</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">simple</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">minimal</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">support</span>
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre
                class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<SimpleHeader />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Simple With Eyebrow Header Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <SimpleWithEyebrowHeader client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Simple With Eyebrow Header</h3>
              <p class="text-muted mb-4">Left-aligned header with eyebrow text, title, and description</p>

              <div class="text-sm space-y-2">
                <div>
                  <strong>AI Reference:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >simple-with-eyebrow-header</code
                  >
                </div>
                <div>
                  <strong>Component:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >SimpleWithEyebrowHeader</code
                  >
                </div>
                <div><strong>Complexity:</strong> <span class="text-green-600">Simple</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">header</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">left-aligned</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">eyebrow</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">branded</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">support</span>
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre
                class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<SimpleWithEyebrowHeader />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Simple With Background Image Header Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <SimpleWithBackgroundImageHeader client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Simple With Background Image Header</h3>
              <p class="text-muted mb-4">Left-aligned header with background image and gradient effects</p>

              <div class="text-sm space-y-2">
                <div>
                  <strong>AI Reference:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >simple-with-background-image-header</code
                  >
                </div>
                <div>
                  <strong>Component:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >SimpleWithBackgroundImageHeader</code
                  >
                </div>
                <div><strong>Complexity:</strong> <span class="text-orange-600">Medium</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">header</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">left-aligned</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">background-image</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">gradient</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">support</span>
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre
                class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<SimpleWithBackgroundImageHeader />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>

      <!-- Centered With Background Image Header Component -->
      <section class="border border-gray-200 dark:border-gray-700 rounded-xl overflow-hidden">
        <!-- Component Preview -->
        <div class="border-b border-gray-200 dark:border-gray-700">
          <CenteredWithBackgroundImageHeader client:load />
        </div>

        <!-- Component Info -->
        <div class="p-6 bg-gray-50 dark:bg-gray-800">
          <div class="grid lg:grid-cols-3 gap-6">
            <div class="lg:col-span-1">
              <h3 class="text-xl font-semibold text-primary mb-2">Centered With Background Image Header</h3>
              <p class="text-muted mb-4">Centered header with background image and gradient effects</p>

              <div class="text-sm space-y-2">
                <div>
                  <strong>AI Reference:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >centered-with-background-image-header</code
                  >
                </div>
                <div>
                  <strong>Component:</strong>
                  <code class="bg-gray-200 dark:bg-gray-700 px-2 py-1 rounded font-mono text-xs"
                    >CenteredWithBackgroundImageHeader</code
                  >
                </div>
                <div><strong>Complexity:</strong> <span class="text-orange-600">Medium</span></div>
              </div>

              <div class="mt-4">
                <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Tags:</h4>
                <div class="flex flex-wrap gap-1">
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">header</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">centered</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">background-image</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">gradient</span>
                  <span class="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">support</span>
                </div>
              </div>
            </div>

            <div class="lg:col-span-2">
              <h4 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">Usage Example:</h4>
              <pre
                class="bg-gray-100 dark:bg-gray-900 p-4 rounded-lg text-xs overflow-x-auto">
                <code>{`<CenteredWithBackgroundImageHeader />`}</code>
              </pre>
            </div>
          </div>
        </div>
      </section>
    </div>

    <!-- Navigation -->
    <div class="mt-12 flex justify-between items-center">
      <a href="/dev-components" class="inline-flex items-center text-primary hover:text-primary/80 font-medium">
        ← Back to Component Library
      </a>
      <a href="/styles-guide" class="inline-flex items-center text-muted hover:text-primary font-medium">
        Styles Guide →
      </a>
    </div>
  </div>
</DevComponentsSidebar>
</Layout>
